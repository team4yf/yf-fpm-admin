import React,{ Component } from 'react'
import {UserList} from './user-list'

export default React.createClass({
  render: () => {
    return (
      <div>
        <UserList />
      </div>
    );
  }
});

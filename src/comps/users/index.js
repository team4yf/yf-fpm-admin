import React,{ Component } from 'react'
import { UserList } from './list'

export default React.createClass({
  render: () => {
    return (
      <div>
        <UserList />
      </div>
    );
  }
});

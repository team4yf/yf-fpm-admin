import React,{ Component } from 'react'
import UserTable from './user-list'

export default React.createClass({
  render: () => {
    return (
      <div>
        <UserTable />
      </div>
    );
  }
});
